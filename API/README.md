## Express API for Fitness App

### How to run
1. Create `.env` file
2. Push schema to database: `npm run db:push`
3. Run the server: `npm run dev`

### API Docs
- `/scalar`: API
- `/api/auth/reference`: Better Auth